sudo: false
cache: bundler
language: ruby
bundler_args: --without kitchen_common kitchen_vagrant
rvm:
  - 2.0
  - 2.1
  - 2.2

bundler_args: --jobs 7

branches:
  only:
    - master

script: bundle exec rake travis:ci
